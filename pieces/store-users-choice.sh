# Processing user's choice: store it in a variable.
selectable-process-users-choice() {

    # Placeholder here is meant to be replaced with a variable name.
    %s="${SELECTABLE_OPTIONS[$SELECTABLE_CURRENT_INDEX]}" 
}
